apiVersion: v1
kind: Service
metadata:
  name: finadvisor-service
  namespace: finadvisor
spec:
  selector:
    app: finadvisor-web
  ports:
    - port: 80
      targetPort: 8000
  type: LoadBalancer
